{
  "entities": {
    "Chat": {
      "title": "Chat",
      "description": "A conversation between the user and the AI",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who owns this chat" },
        "title": { "type": "string", "description": "The title of the chat, usually derived from the first message" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the chat was created" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "When the chat was last updated" }
      },
      "required": ["userId", "title", "createdAt", "updatedAt"]
    },
    "Message": {
      "title": "Message",
      "description": "A single message within a chat",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "model"], "description": "The role of the sender" },
        "text": { "type": "string", "description": "The content of the message" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the message was sent" }
      },
      "required": ["role", "text", "createdAt"]
    }
  },
  "firestore": {
    "chats/{chatId}": {
      "schema": "Chat",
      "description": "Root collection for chats"
    },
    "chats/{chatId}/messages/{messageId}": {
      "schema": "Message",
      "description": "Subcollection for messages within a specific chat"
    }
  }
}
